"""Repository maintenance and validation scripts.

This explicit package marker keeps local ``scripts.*`` imports deterministic
when optional Windows dependencies install their own top-level package with the
same generic name.
"""
