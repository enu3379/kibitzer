{
  "name": "kibitzer-extension-next",
  "private": true,
  "type": "module",
  "description": "Serverless (TypeScript-only) Kibitzer extension. Built in parallel with apps/extension; supersedes it and apps/server at cutover.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "assets:check": "node scripts/checkProviderAssets.mjs",
    "test": "node --experimental-strip-types --test && npm run typecheck && npm run typecheck:test",
    "build": "npm run assets:check && npm run test && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@huggingface/tokenizers": "0.1.3",
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0"
  }
}
