{
  "name": "kibitzer-extension",
  "private": true,
  "type": "module",
  "description": "The serverless, TypeScript-only Kibitzer Chrome extension.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "assets:check": "node scripts/checkProviderAssets.mjs",
    "test": "node --experimental-strip-types --test && npm run typecheck && npm run typecheck:test",
    "build": "npm run assets:check && npm run test && node build.mjs",
    "build:dev": "npm run assets:check && npm run test && node build.mjs --dev",
    "watch": "node build.mjs --watch --dev"
  },
  "dependencies": {
    "@huggingface/tokenizers": "0.1.3",
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.5.0"
  }
}
