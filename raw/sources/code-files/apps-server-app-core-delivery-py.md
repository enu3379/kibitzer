---
type: code
title: "apps/server/app/core/delivery.py"
source_path: "apps/server/app/core/delivery.py"
generated: true
related: [code-map-pages]
---

# apps/server/app/core/delivery.py

Generated code search source for LLM Wiki. Do not edit this file directly.

Related: [[code-map-pages]] [[server-core-pipeline-module]] [[observation-pipeline]]

```python
from __future__ import annotations


def clamp_notification_message(message: str, max_sentences: int) -> str:
    text = " ".join(message.split())
    if max_sentences <= 0:
        return text

    sentences: list[str] = []
    start = 0
    for index, char in enumerate(text):
        if char in ".!?。！？":
            sentence = text[start : index + 1].strip()
            if sentence:
                sentences.append(sentence)
            start = index + 1
            if len(sentences) >= max_sentences:
                return " ".join(sentences)

    tail = text[start:].strip()
    if tail and len(sentences) < max_sentences:
        sentences.append(tail)
    return " ".join(sentences)

```
