---
type: code
title: "apps/server/app/core/controllers/streak.py"
source_path: "apps/server/app/core/controllers/streak.py"
generated: true
related: [code-map-pages]
---

# apps/server/app/core/controllers/streak.py

Generated code search source for LLM Wiki. Do not edit this file directly.

Related: [[code-map-pages]] [[server-core-pipeline-module]] [[observation-pipeline]]

```python
from dataclasses import dataclass
from datetime import datetime, timedelta

from ...schemas import Verdict


@dataclass
class StreakController:
    k: int = 3
    cooldown_seconds: int = 300
    coldstart_observations: int = 5
    streak: int = 0
    obs_count: int = 0
    last_intervention_ts: datetime | None = None
    snoozed_until: datetime | None = None

    def update(self, verdict: Verdict, r: float | None = None) -> None:
        self.obs_count += 1
        if verdict == Verdict.OK:
            self.streak = 0
            return
        self.streak += 1

    def should_intervene(self, now: datetime) -> bool:
        if self.obs_count < self.coldstart_observations:
            return False
        if self.streak < self.k:
            return False
        if self.snoozed_until and now < self.snoozed_until:
            return False
        if self.last_intervention_ts:
            cooldown_until = self.last_intervention_ts + timedelta(seconds=self.cooldown_seconds)
            if now < cooldown_until:
                return False
        return True

    def on_intervened(self, now: datetime) -> None:
        self.streak = 0
        self.last_intervention_ts = now

    def on_feedback(self, kind: str) -> None:
        if kind == "relevant":
            self.streak = 0


```
