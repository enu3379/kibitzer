{
  "name": "kibitzer-workspace",
  "private": true,
  "scripts": {
    "extension:build": "npm --prefix apps/extension-next run build",
    "extension:test": "npm --prefix apps/extension-next run test"
  }
}
